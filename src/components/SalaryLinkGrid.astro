---
import { salaryTargets } from '../utils/salaryTargets';
---
<section class="card" aria-labelledby="salary-links-heading">
  <h2 id="salary-links-heading">Popular salary after-tax lookups</h2>
  <p class="section-copy">
    Jump straight to a pre-filled calculator page for the salary you are researching. Each page shows take-home pay, income tax, and National Insurance totals for 2025/26.
  </p>
  <div class="link-grid">
    {salaryTargets.map((salary) => (
      <a class="link-chip" href={`/salary-tax-calculator/${salary}-after-tax/`}>
        £{salary.toLocaleString('en-GB')} salary
      </a>
    ))}
  </div>
</section>

<style>
  .link-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 1.25rem;
  }

  .link-chip {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.65rem 1.15rem;
    border-radius: 999px;
    border: 1px solid rgba(148, 163, 184, 0.4);
    background: rgba(148, 163, 184, 0.12);
    color: inherit;
    font-weight: 600;
    transition: border-color 0.2s ease, transform 0.2s ease;
  }

  .link-chip:hover {
    border-color: rgba(29, 78, 216, 0.6);
    transform: translateY(-1px);
    text-decoration: none;
  }
</style>
